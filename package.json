{
  "name": "rate-limiter",
  "version": "0.1.0",
  "description": "Token bucket and sliding window rate limiter library with TypeScript",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --verbose",
    "lint": "tsc --noEmit"
  },
  "keywords": ["rate-limit", "token-bucket", "sliding-window", "throttle"],
  "author": "Nripanka Das",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.0"
  }
}
